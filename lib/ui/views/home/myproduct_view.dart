import 'package:flutter/material.dart';

class MyProductView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('myproduct'),
      ),
    );
  }
}
